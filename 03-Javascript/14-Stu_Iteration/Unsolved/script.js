//WRITE YOUR CODE BELOW
var students = ["Jimmy", "Cindy", "Afif", "Priti", "Eustace"];

const studentCount = (array) => array.length;

console.log(studentCount(students));

// log a greeting for each student

students.forEach((element) => console.log("Great to see you, " + element));
